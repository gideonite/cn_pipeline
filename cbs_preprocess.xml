<tool id="cbs_preprocess" name="CBS preprocess">
    <description>gets data ready to be sent through CBS</description>
    <command interpreter="python">probe_utils.py join_probe_signal $log2ratio $markersfile</command>
    <inputs>
        <param format="tabular" name="log2ratio" type="data" label="log2 ratio data (TCGA Level 2)"/>
        <param format="tabular" name="markersfile" type="data" label="markersfile (just like GISTIC input)"/>
    </inputs>
    <outputs>
        <data format="tabular" name="cbs_in" />
    </outputs>
    <help>
        This preprocessing script joins a log2 ratio data (corresponding to
        TCGA's Level 2 data) and adds in chr and locus columns.  This is the
        input to CBS.
    </help>
</tool>
